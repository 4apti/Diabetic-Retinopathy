# Core API dependencies (auth + upload + quality gate).
# ML heavy deps are split into requirements-ml.txt because torch/opencv/ultralytics
# are optional at runtime — the API degrades gracefully when they are missing.
# Exact versions are pinned per spec for reproducible judges' setups.
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
bcrypt==5.0.0
pyjwt==2.13.0
python-multipart==0.0.32
pillow==12.3.0
numpy==2.5.3
edge-tts==7.2.8